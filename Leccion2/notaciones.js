// Notación

// ; -> Delimitar el final de una linea
//const b = 4

// . -> Se utiliza en los objetos para acceder a los atributos
// movil.anchura

// [] -> listas, arreglos o arrays
const a = [1, 2, 3, 4]
console.log(a[2])

// () -> funciones
function suma(a, b) {
    // aqui se escribiria la funcion
}

// {} -> llaves para objetos, funciones y estructuras de control

const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
    //contenido aqui

    const c = "hola"
}